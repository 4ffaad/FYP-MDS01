# Optional CPU video runtime; the normal EEG/backend image keeps its dependencies.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu
timm==1.0.15
pycocotools==2.0.8
